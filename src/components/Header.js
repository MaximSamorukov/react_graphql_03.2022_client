import { Button } from "antd";
import './Header.css';

const Header = () => {
  return (
    <div className="header">
      <Button type="primary">Add</Button>
    </div>
  )
}

export default Header;